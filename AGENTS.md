# 项目协作与实施规则

## 设计先行

- 对任何会修改业务代码、配置、部署拓扑、数据结构或外部资源的需求，先在 `docs/` 提供设计方案；方案至少包含目标、边界、架构、变更清单、验收标准、风险与回滚方案。
- 在用户明确确认设计前，只允许进行只读检查、方案文档和知识沉淀；不得修改实现代码、运行配置、容器编排或启动外部服务。
- 获确认后，按小步提交实施。每个阶段完成后记录实际变更、验证命令、结果和遗留风险。
- 设计方案必须从真实网盘业务场景出发，明确用户动作、数据流、失败路径、异常兜底、数据一致性边界和可观测性要求；不得只描述组件拼装。

## 问题闭环与知识沉淀

- 每次解决故障后，必须在项目 `docs/` 下沉淀文档，写清楚发生原因、排查过程、解决方案，以及让新同事能够看懂所需的前置知识。
- 每次改进代码后，必须在项目 `docs/` 下沉淀文档，写清楚为什么改进、怎么改、原理、实现，以及让新同事能够看懂所需的前置知识。
- 每个实施中发现的问题均须记录：现象、影响范围、根因、方案选择、实际修改、验证结果和可复用经验。
- 新增或调整基础设施能力时，必须同步维护对应的设计文档与实施复盘文档。
- 文档中不得记录真实密钥、Token、密码、个人邮箱或可直接访问的内部地址；使用占位符说明配置方式。

## 设计与验收职责隔离

- 设计、实施、验收必须由不同职责的 Agent 负责；设计 Agent 不得编写自身方案的验收结论，验收 Agent 不得参与该方案的设计决策。
- 验收 Agent 应基于独立验收文档和明确通过标准执行复核，重点检查异常路径、兜底行为、数据一致性与证据完整性。
- 未通过独立验收时，不得把功能标注为完成，也不得在简历或项目说明中将设计目标描述为已实现结果。

## Git 提交规范

- `main` 保持可运行；功能开发使用 `codex/<feature>` 分支并通过 PR 合并。
- 禁止提交构建产物、本地工具目录、日志、压测原始结果和任何真实密钥。
- 提交前执行 `git status`，只暂存与当前目标相关的文件。
